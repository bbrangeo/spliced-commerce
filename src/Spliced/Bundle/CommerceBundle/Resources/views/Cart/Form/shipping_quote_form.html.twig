<h3>Shipping Options</h3>
{% if shippingQuoteForm is defined %}
<div class="alert alert-block alert-info">
    <ul class="inline shipping-quote-form">
	    <li><strong>Where is your order going?</strong></li>
		<li>{{ form_widget(shippingQuoteForm.country, { 'attr' : { 'class' : 'selectpicker', } }) }}</li>
		<li>{{ form_widget(shippingQuoteForm.zipcode, { 'attr' : { 'class' : 'span2', 'placeholder' : 'Zipcode' } }) }}</li>
		<li><button type="submit" class="btn btn-info">{{ shippingOptions|length ? 'Update Quote' : 'Get Quote' }}</button></li>
	</ul>
</div>
{% endif %}

{% if shippingOptions|length %}
<table class="table table table-striped table-bordered cart-shipping-table">
    {% for method in shippingOptions %}
	<tr>
	    <td><i class="icon-plane"></i></td>
		<td>{{ method.label }} <small>{{ method.label2 }}</small></td>
		<td>${{ method.price }}</td>
	</tr>
	{% endfor %}
</table>
{% endif %}
